# Cloud Synchronizer – environment variables
# Copy this file to .env.local and fill in real values. Never commit secrets.

# --- OpenRouter (LLM for DiskDetectionAgent) ---
# Get your key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Model to use (default: google/gemini-2.0-flash-001 – cheap & smart, supports tool calling)
# Other good options:
#   mistralai/mistral-small-3.1-24b-instruct
#   anthropic/claude-3-5-haiku-20241022
#   meta-llama/llama-3.3-70b-instruct
OPENROUTER_MODEL=google/gemini-2.0-flash-001

# --- App credentials ---
APP_USERNAME=admin
APP_PASSWORD=changeme

# --- Paths (host-side sources for bind mounts) ---
DB_PATH_HOST=/opt/cloudsync/db
EXTERNAL_DRIVE_PATH_HOST=/mnt/external-drive
IPHONE_HOST_MOUNT_PATH=/mnt/iphone

# --- Paths (container-side mount points) ---
EXTERNAL_DRIVE_PATH_CONTAINER=/mnt/external-drive
IPHONE_MOUNT_PATH_CONTAINER=/mnt/iphone

# --- iCloud microservice ---
ICLOUD_SERVICE_URL=http://icloud-service:8000
DB_PATH_HOST=./backend/data
